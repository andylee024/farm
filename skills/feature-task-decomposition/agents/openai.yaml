interface:
  display_name: "Feature Task Decomposition"
  short_description: "Decompose feature intent into atomic Linear child tasks and execute one approved task at a time with Farm."
  default_prompt: "Use this skill to break feature intent into atomic tasks, approve one child in Linear, and run it with farm run/update/finish/status."
